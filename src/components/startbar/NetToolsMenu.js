import React from 'react'

const NetToolsMenu = (props) => {
    
    if (props.isClicked) {
        return (
            <div>NetToolsMenu</div>
          )
    } else {
        return null
    }

}

export default NetToolsMenu